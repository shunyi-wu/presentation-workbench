# 工作台原则

- 固定 `1600x900` 画布，页面内部不响应式重排。
- 每个 deck 优先自包含：内容、样式、资产和 review 输出都放在 `decks/<deck-id>/`。
- 工作台负责运行、导航、标注、截图和检查；具体主题内容不进入 `src/`。
- 自动检查只能发现部分问题，最终仍以截图 review 为准。
- 内置 `placeholder-blank-*` 是占位空白 PPT，用来展示工作台收纳效果，不是正式内容模版；用户理解后可以删除。
