# 批量重排任务模板

请根据现有蓝图小批次重排或新增指定页面。

工作顺序：

1. 先理解本批页面的内容目标，再决定每页版式。
2. 已验证模板只作为参考，不允许机械套用。
3. 版面服务内容；文字、代码和层级能讲清楚时，不强行添加组件。
4. 只有确实需要互动、结构图、状态图或图表时，才放按实际尺寸预留的区域。
5. 本批完成后按影响范围运行验证命令；页面渲染相关修改通常需要 `npm run check` 和 `npm run review`。
